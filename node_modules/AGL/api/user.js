"use strict";
//==============================================================================
//  Modules
//==============================================================================

var userFactory = require('AGL/user/factory');

//==============================================================================
//  Public Functions
//==============================================================================

module.exports.CreateUser = function(properties, callback) {
    if (typeof callback !== 'function') throw("Callback argument must be a function.");

    // Test callback
    callback(null, { publicUserId : '123' });

    userFactory.NewUser(properties, function UserCreated(error, user) {
        // TODO IF error call callback with error else save user
    });
};

//==============================================================================
//  Public Functions
//==============================================================================

function SaveUser(user, callback) {
    // TODO Save user to database using user repository
    // If error call callback with error else call callback without error
}
