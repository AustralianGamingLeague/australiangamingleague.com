"use strict";
//=========================================================
//  Modules
//=========================================================


//=========================================================
//  Exports
//=========================================================

module.exports = function(app) {

    // API routes
    app.get('/api/test', function(request, response) {
        response.json("This is just a test.");
    });

    // TODO Read up on RESTful APIs before going further. eg. This should be a POST, not GET
    app.get('/api/user', function(request, response) {
        userAPI.CreateUser(properties, function UserCreated(error) {
            // TODO IF error send back standardized error else return 200 success
        });
    });

    // Front end routes

    app.get('*', function(request, response) {
        response.sendFile('index.html', {'root' : './public'});
    });
};
