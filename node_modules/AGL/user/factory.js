"use strict";
//==============================================================================
//  Modules
//==============================================================================

//==============================================================================
//  Constants
//==============================================================================

var USER_PROPERTIES = {
};

//==============================================================================
//  Public Functions
//==============================================================================

//  Creates a new User object.
//  Will send an error message and a user object as
//  arugments to the callback: callback(error, user).
//
//  @param {object} properties required for a user.
//  @param {function} callback
//
module.exports.NewUser = function(properties, callback) {
    if (typeof callback !== 'function') throw("Callback argument must be a function.");

    // TODO Validate properties
    // If validation fails send an error message in callback
    // else create new User() and send that back
};

//==============================================================================
//  Private Functions
//==============================================================================

function ValidateProperties(properties) {
    // TODO Validate the things
}

//  User Class.
//  Represents a User.
//
//  @param {object} properties required for a user.
//  @return {object} user object
//
function User(properties) {
}
